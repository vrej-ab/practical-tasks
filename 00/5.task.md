### `/bin/sh` VS `/bin/bash`
* What is the difference between `/bin/sh` and `/bin/bash` Shebangs?
* How to find the shell/interpreter linked to `/bin/sh`?
* How to run a command in sub-shell instead of running it in the current shell?

